export function markers(monaco: any, diagnostics: any[]) { return diagnostics.map((item) => ({ severity: item.severity === "warning" ? monaco.MarkerSeverity.Warning : monaco.MarkerSeverity.Error, message: `${item.message}${item.suggestion ? `\n${item.suggestion}` : ""}`, startLineNumber: item.line, startColumn: item.column, endLineNumber: item.endLine || item.line, endColumn: item.endColumn || item.column + 1 })); }
